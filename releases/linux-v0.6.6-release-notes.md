# dosforge linux-v0.6.6 — companion to windows-v0.6.6

No functional changes for Linux. Linux artifacts for v0.6.6 are
re-published in lock-step with the Windows CLI bundle (which had to
be re-tagged because the v0.6.5 CI smoke check was overly strict and
blocked CLI publication).

See `linux-v0.6.5-release-notes.md` for the actual feature changes
(PC-DOS 2000 hydration accepts raw `disk01.img`..`disk06.img` in
`dosassets/pcdos2000/`).
