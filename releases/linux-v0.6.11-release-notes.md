# dosforge v0.6.11 — Linux

Parity bump with Windows v0.6.11. Verified `mcopy ... :: .` works
identically to the previous `mcopy ... :: ./` on Linux mtools, so the
change is a no-op there.

See `releases/windows-v0.6.11-release-notes.md` for the Windows story.
