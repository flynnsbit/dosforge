# dosforge v0.6.8 — Linux

Version bump to keep Linux + Windows release tags in lockstep with the
v0.6.8 Windows PyInstaller spec fix. No runtime Linux changes — the fix
is entirely inside `windows/dosforge.spec`.

See `releases/windows-v0.6.8-release-notes.md` for details.
